---
phase: 01-detail-pane-mode-switching
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/systemd.rs
autonomous: true
requirements:
  - DTL-03
must_haves:
  truths:
    - "Service File mode has a real data source for the selected timer's service unit."
    - "Missing or unreadable unit files return readable fallback text instead of blank output."
    - "Service-file fetching can be re-run during periodic refresh without adding UI-specific branching to the system layer."
  artifacts:
    - path: "src/systemd.rs"
      provides: "Service unit file fetch helper"
    - path: "src/systemd.rs"
      provides: "Unit tests for command-output normalization and fallback text"
  key_links:
    - from: "src/main.rs"
      to: "src/systemd.rs"
      via: "detail content refresh"
      pattern: "fetch_service_file_content"
---

<objective>
Add the systemd integration needed to show readable service unit file contents in the detail pane.

Purpose: DTL-03 requires a concrete data source for `Service File` mode; this plan creates it behind a stable helper API before the UI and input flow depend on it.
Output: A `src/systemd.rs` helper that fetches service-unit text and tests covering fallback formatting.
</objective>

<execution_context>
@/home/christopher/.codex/get-shit-done/workflows/execute-plan.md
@/home/christopher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/systemd.rs
@src/main.rs

<interfaces>
From src/systemd.rs:
```rust
pub async fn fetch_timer_status(timer_unit: &str) -> String
pub async fn fetch_timer_logs(service_unit: &str) -> String

pub struct TimerInfo {
    pub unit: String,
    pub activates: String,
    pub next_abs: String,
    pub last_abs: String,
    pub next_rel: String,
    pub last_rel: String,
    pub status: String,
    pub schedule: String,
}
```

From src/main.rs:
```rust
let unit = timer.unit.clone();
let activates = timer.activates.clone();
app.detail_status = fetch_timer_status(&unit).await;
app.detail_logs = fetch_timer_logs(&activates).await;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement service-file fetching for the selected timer service unit</name>
  <files>src/systemd.rs</files>
  <read_first>src/systemd.rs, src/main.rs, .planning/REQUIREMENTS.md, README.md</read_first>
  <behavior>
    - Test 1: a selected timer can request readable service file content using its `activates` unit per DTL-03.
    - Test 2: empty stdout or command failure returns explicit fallback text instead of an empty string.
  </behavior>
  <action>Add a new async function named `fetch_service_file_content(service_unit: &str) -> String` in `src/systemd.rs`. Invoke `systemctl --user cat <service_unit> --no-pager` with `tokio::process::Command`, return stdout when the command succeeds and stdout is non-empty, and otherwise return an explicit message string starting with `Service file unavailable:` followed by stderr text, the IO error, or `empty output`. Add a small pure helper named `normalize_service_file_output(stdout: &[u8], stderr: &[u8], success: bool) -> String` so the behavior is unit-testable without spawning subprocesses. Do not add a new module or dependency; keep this logic beside the existing `fetch_timer_logs` helper.</action>
  <verify>
    <automated>cargo test systemd::tests -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `src/systemd.rs` contains `pub async fn fetch_service_file_content(service_unit: &str) -> String`.
    - `src/systemd.rs` contains `normalize_service_file_output`.
    - `src/systemd.rs` contains the exact string `Service file unavailable:`.
    - `src/systemd.rs` contains `args(&["--user", "cat", service_unit, "--no-pager"])`.
  </acceptance_criteria>
  <done>`src/systemd.rs` exposes a dedicated helper for Service File mode and always returns readable text for success, empty output, or failure cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add unit tests for service-file normalization and fallback behavior</name>
  <files>src/systemd.rs</files>
  <read_first>src/systemd.rs, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</read_first>
  <behavior>
    - Test 1: successful non-empty stdout is returned unchanged.
    - Test 2: failed command output becomes `Service file unavailable: ...`.
    - Test 3: successful empty stdout becomes `Service file unavailable: empty output`.
  </behavior>
  <action>Add a `#[cfg(test)] mod tests` block in `src/systemd.rs` or extend one if created in the same file. Include unit tests named `service_file_output_preserves_successful_stdout`, `service_file_output_reports_command_stderr`, and `service_file_output_reports_empty_success_output`. Exercise `normalize_service_file_output` directly with byte literals so `cargo test systemd::tests` proves the DTL-03 fallback contract without requiring live `systemctl` access.</action>
  <verify>
    <automated>cargo test systemd::tests -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `src/systemd.rs` contains `service_file_output_preserves_successful_stdout`.
    - `src/systemd.rs` contains `service_file_output_reports_command_stderr`.
    - `src/systemd.rs` contains `service_file_output_reports_empty_success_output`.
    - `src/systemd.rs` contains `#[cfg(test)]`.
  </acceptance_criteria>
  <done>The service-file data path is covered by fast unit tests that lock in readable fallback behavior for DTL-03.</done>
</task>

</tasks>

<verification>
- [ ] `cargo test systemd::tests -- --nocapture`
- [ ] `cargo build`
- [ ] `rg -n "fetch_service_file_content|normalize_service_file_output|Service file unavailable:" src/systemd.rs`
</verification>

<success_criteria>
- `Service File` mode has a dedicated systemd fetch helper.
- Command errors and empty output render as explicit text rather than blank panels.
- Unit tests cover the normalization rules without relying on external system state.
</success_criteria>

<output>
After completion, create `.planning/phases/01-detail-pane-mode-switching/01-02-SUMMARY.md`
</output>
