---
phase: 01-detail-pane-mode-switching
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/main.rs
  - src/ui.rs
  - README.md
autonomous: true
requirements:
  - DTL-01
  - NAV-01
  - NAV-02
  - NAV-03
  - UI-01
  - UI-02
must_haves:
  truths:
    - "Opening detail view shows Logs in the bottom pane immediately."
    - "Tab visibly switches active focus between the top and bottom panes."
    - "Arrow keys change bottom-pane mode only when the top pane is active."
    - "When the bottom pane is active, Left, Right, Up, and Down all act as scroll keys and do not exit detail view."
    - "The UI always labels whether the bottom pane is showing Logs or Service File."
  artifacts:
    - path: "src/main.rs"
      provides: "Detail-view key handling and periodic refresh wiring"
    - path: "src/ui.rs"
      provides: "Focused pane styling and mode labels"
    - path: "README.md"
      provides: "Updated keybinding reference for detail view"
  key_links:
    - from: "src/main.rs"
      to: "src/app.rs"
      via: "focus/mode/scroll helper calls"
      pattern: "toggle_detail_focus|select_next_detail_content|scroll_detail_down"
    - from: "src/main.rs"
      to: "src/systemd.rs"
      via: "mode-specific bottom-pane fetches"
      pattern: "fetch_timer_logs|fetch_service_file_content"
    - from: "src/ui.rs"
      to: "src/app.rs"
      via: "active pane and mode rendering"
      pattern: "detail_focus|detail_content_mode|detail_scroll"
---

<objective>
Wire the new detail interaction model into the runtime and render the active pane and content mode clearly in the terminal UI.

Purpose: This plan turns the state contract and service-file fetch helper into the actual user-visible workflow promised by Phase 1.
Output: Updated input handling, mode-aware refresh behavior, visible pane/mode indicators, and user docs for the new keybindings.
</objective>

<execution_context>
@/home/christopher/.codex/get-shit-done/workflows/execute-plan.md
@/home/christopher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/app.rs
@src/main.rs
@src/systemd.rs
@src/ui.rs
@README.md

<interfaces>
From src/app.rs after Plan 01:
```rust
pub enum DetailPaneFocus { Top, Bottom }
pub enum DetailContentMode { Logs, ServiceFile }

pub struct App {
    pub detail_focus: DetailPaneFocus,
    pub detail_content_mode: DetailContentMode,
    pub detail_status: String,
    pub detail_logs: String,
    pub detail_scroll: usize,
}

pub fn toggle_detail_focus(&mut self)
pub fn select_next_detail_content(&mut self)
pub fn select_previous_detail_content(&mut self)
pub fn scroll_detail_down(&mut self, max_lines: usize)
pub fn scroll_detail_up(&mut self)
```

From src/systemd.rs after Plan 02:
```rust
pub async fn fetch_timer_logs(service_unit: &str) -> String
pub async fn fetch_service_file_content(service_unit: &str) -> String
pub async fn fetch_timer_status(timer_unit: &str) -> String
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire detail-view key handling and content refresh around pane focus</name>
  <files>src/main.rs</files>
  <read_first>src/main.rs, src/app.rs, src/systemd.rs, .planning/STATE.md, .planning/REQUIREMENTS.md</read_first>
  <action>Update `src/main.rs` so opening detail view loads `app.detail_status` from `fetch_timer_status(&unit)`, loads `app.detail_logs` from `fetch_timer_logs(&activates)`, then calls `app.enter_detail()` and leaves `detail_content_mode` at `Logs` per DTL-02 from Plan 01. In `ViewMode::Detail`, bind `KeyCode::Tab` to `app.toggle_detail_focus()` per NAV-01. When `app.detail_focus` is `DetailPaneFocus::Top`, map `Left` and `Up` to `app.select_previous_detail_content()`, and `Right` and `Down` to `app.select_next_detail_content()` per NAV-02. After a mode change, reload `app.detail_logs` with `fetch_timer_logs(&activates)` for `Logs` or `fetch_service_file_content(&activates)` for `ServiceFile`, and reset `app.detail_scroll = 0`. When `app.detail_focus` is `DetailPaneFocus::Bottom`, define the full arrow-key contract per NAV-03: `Up` and `Left` call `app.scroll_detail_up()`, while `Down` and `Right` call `app.scroll_detail_down(max_lines)`, with `max_lines` derived from the currently rendered bottom-pane content. Keep `k` mapped to `app.scroll_detail_up()` and `j` mapped to `app.scroll_detail_down(max_lines)` in the bottom pane as equivalent vim-style scroll keys. Do not allow `Left` to exit detail view while the bottom pane is active, and do not leave `Right` unspecified in that state. Keep `Esc` and `Backspace` as the only unconditional detail-exit keys regardless of active pane. During the 60-second refresh branch, reload `app.detail_status` and refresh the current bottom content according to `app.detail_content_mode` rather than always replacing it with logs.</action>
  <verify>
    <automated>cargo test && cargo build && rg -n "KeyCode::Tab|KeyCode::Left|KeyCode::Right|KeyCode::Esc|KeyCode::Backspace|DetailPaneFocus::Bottom|scroll_detail_up|scroll_detail_down" src/main.rs</automated>
  </verify>
  <acceptance_criteria>
    - `src/main.rs` contains `KeyCode::Tab`.
    - `src/main.rs` contains `app.toggle_detail_focus()`.
    - `src/main.rs` contains `fetch_service_file_content(&activates)`.
    - `src/main.rs` contains `DetailPaneFocus::Top`.
    - `src/main.rs` contains `DetailPaneFocus::Bottom`.
    - `src/main.rs` explicitly maps bottom-pane `KeyCode::Left` to scrolling, not detail exit.
    - `src/main.rs` explicitly maps bottom-pane `KeyCode::Right` to scrolling.
    - `src/main.rs` keeps `KeyCode::Esc` and `KeyCode::Backspace` as detail-exit bindings.
    - `src/main.rs` no longer uses `KeyCode::Left` as an unconditional detail-view exit branch.
  </acceptance_criteria>
  <done>The event loop enforces DTL-01, NAV-01, NAV-02, and NAV-03 with a fully specified pane-aware keymap: top-pane arrows switch content mode, bottom-pane Left/Right/Up/Down and `j/k` scroll content, and only `Esc`/`Backspace` exit detail view.</done>
</task>

<task type="auto">
  <name>Task 2: Render active pane and mode labels and document the final keybindings</name>
  <files>src/ui.rs, README.md</files>
  <read_first>src/ui.rs, README.md, src/app.rs, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</read_first>
  <action>Update `draw_detail` in `src/ui.rs` to render the top pane with a title that includes the active mode selector text exactly as `Detail Controls [Logs | Service File]`, highlighting the selected mode with a distinct style tied to `app.detail_content_mode`. Change the bottom pane title to `Bottom Pane: Logs` or `Bottom Pane: Service File` from the enum value so UI-02 is always visible. Apply one border color for the active pane and a different muted color for the inactive pane based on `app.detail_focus` so UI-01 is visible without guessing. Render bottom-pane content from `app.detail_logs` regardless of whether it currently holds logs or service file text, and explicitly feed `app.detail_scroll` into the bottom-pane `Paragraph`/rendering path so the visible viewport scrolls in both `Logs` and `Service File` mode instead of only updating state in `App`. Keep wrapping readable for multi-line unit files while preserving scroll offset behavior across redraws. Update the README keybindings section with exact bullets for `Tab`, `Left/Right/Up/Down` when the top pane is active, `Left/Right/Up/Down` as scroll keys when the bottom pane is active, `j/k` as equivalent bottom-pane scrolling, and `Esc`/`Backspace` as the detail-exit keys.</action>
  <verify>
    <automated>cargo test && cargo build && rg -n "detail_scroll|scroll\\(" src/ui.rs && rg -n "Tab|Left/Right/Up/Down|Esc|Backspace|Service File" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui.rs` contains `Detail Controls [Logs | Service File]`.
    - `src/ui.rs` contains `Bottom Pane: Logs`.
    - `src/ui.rs` contains `Bottom Pane: Service File`.
    - `src/ui.rs` contains `detail_focus`.
    - `src/ui.rs` contains `detail_scroll`.
    - `src/ui.rs` applies `detail_scroll` in the bottom-pane rendering path for both Logs and Service File mode.
    - `README.md` contains `` `Tab` ``.
    - `README.md` contains `Service File`.
    - `README.md` documents `Left/Right/Up/Down` as bottom-pane scrolling keys.
    - `README.md` documents `Esc` and `Backspace` as detail-exit keys.
  </acceptance_criteria>
  <done>The detail UI makes the active pane and current bottom-pane mode obvious, visibly scrolls the rendered bottom-pane viewport using `app.detail_scroll` in both Logs and Service File mode, and the README documents the same explicit keymap the code implements, including bottom-pane arrow-key scrolling and `Esc`/`Backspace` exit behavior.</done>
</task>

</tasks>

<verification>
- [ ] `cargo test`
- [ ] `cargo build`
 - [ ] `rg -n "KeyCode::Tab|KeyCode::Left|KeyCode::Right|KeyCode::Esc|KeyCode::Backspace|fetch_service_file_content|Detail Controls \\[Logs \\| Service File\\]|Bottom Pane: Logs|Bottom Pane: Service File|detail_scroll|scroll\\(" src/main.rs src/ui.rs README.md`
 - [ ] Confirm the documented contract matches NAV-03 exactly: with bottom-pane focus, `Left`, `Right`, `Up`, and `Down` scroll content and do not change mode or exit detail view; `Esc` and `Backspace` remain available as exit keys.
</verification>

<success_criteria>
- Users open detail view and immediately see Logs in the bottom pane.
- Users can switch focus with `Tab` and always see which pane is active.
- Users can switch between `Logs` and `Service File` from the top pane and scroll the current content from the bottom pane.
- Users get a complete bottom-pane keymap contract: `Left/Right/Up/Down` and `j/k` scroll, while `Esc` and `Backspace` exit.
- Scrolling produces visible movement in the bottom pane for both `Logs` and `Service File` content.
- The UI labels the active content mode in text, not color alone.
</success_criteria>

<output>
After completion, create `.planning/phases/01-detail-pane-mode-switching/01-03-SUMMARY.md`
</output>
