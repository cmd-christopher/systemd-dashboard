---
phase: 01-detail-pane-mode-switching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app.rs
autonomous: true
requirements:
  - DTL-02
  - NAV-01
  - NAV-02
  - NAV-03
must_haves:
  truths:
    - "Entering detail view initializes the bottom pane in Logs mode."
    - "Detail view tracks which pane is active before key handling runs."
    - "Top-pane navigation and bottom-pane scrolling use separate state paths."
  artifacts:
    - path: "src/app.rs"
      provides: "Detail pane focus and content-mode state transitions"
    - path: "src/app.rs"
      provides: "Unit tests covering detail-mode defaults and navigation helpers"
  key_links:
    - from: "src/main.rs"
      to: "src/app.rs"
      via: "detail-view key handling methods"
      pattern: "app\\.(enter_detail|toggle_detail_focus|select_next_detail_content|scroll_detail_content)"
---

<objective>
Define the detail-view state contract so the executor can wire mode switching without inventing behavior mid-implementation.

Purpose: Put the keyboard rules from Phase 1 into `App` methods and tests before the event loop or UI start depending on them.
Output: `App` detail-pane enums/fields/helpers plus deterministic unit coverage for the new state transitions.
</objective>

<execution_context>
@/home/christopher/.codex/get-shit-done/workflows/execute-plan.md
@/home/christopher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/app.rs
@src/main.rs

<interfaces>
From src/app.rs:
```rust
pub enum ViewMode {
    List,
    Detail,
}

pub struct App {
    pub timers: Vec<TimerInfo>,
    pub mode: ViewMode,
    pub selected_index: usize,
    pub detail_status: String,
    pub detail_logs: String,
    pub should_quit: bool,
    pub error: Option<String>,
    pub detail_scroll: usize,
}

pub fn enter_detail(&mut self)
pub fn exit_detail(&mut self)
pub fn selected_timer(&self) -> Option<&TimerInfo>
```

From src/main.rs:
```rust
KeyCode::Enter => { ... app.detail_status = fetch_timer_status(&unit).await; app.detail_logs = fetch_timer_logs(&activates).await; ... app.enter_detail(); }
ViewMode::Detail => match key.code {
    KeyCode::Down | KeyCode::Char('j') => { ... }
    KeyCode::Up | KeyCode::Char('k') => { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add detail-pane state enums and transition helpers in App</name>
  <files>src/app.rs</files>
  <read_first>src/app.rs, src/main.rs, .planning/STATE.md, .planning/REQUIREMENTS.md</read_first>
  <behavior>
    - Test 1: entering detail mode resets focus to the top pane and selects `Logs` by default per DTL-02.
    - Test 2: `Tab` toggles active pane between top and bottom without leaving detail mode per NAV-01.
    - Test 3: top-pane arrow helpers change content mode between `Logs` and `Service File`, while bottom-pane scroll helpers only change scroll offset per NAV-02 and NAV-03.
  </behavior>
  <action>Add `DetailPaneFocus` with variants `Top` and `Bottom`, add `DetailContentMode` with variants `Logs` and `ServiceFile`, and store both on `App` as public fields named `detail_focus` and `detail_content_mode`. Replace the current `enter_detail()` behavior with a reset that sets `mode = ViewMode::Detail`, `detail_focus = DetailPaneFocus::Top`, `detail_content_mode = DetailContentMode::Logs`, and `detail_scroll = 0` per DTL-02. Add explicit helper methods on `App` named `toggle_detail_focus`, `select_next_detail_content`, `select_previous_detail_content`, `scroll_detail_down(max_lines: usize)`, and `scroll_detail_up()` so `src/main.rs` can call deterministic transitions instead of mutating raw fields directly. Keep `exit_detail()` clearing detail strings and also restore `detail_focus = DetailPaneFocus::Top`, `detail_content_mode = DetailContentMode::Logs`, and `detail_scroll = 0`.</action>
  <verify>
    <automated>cargo test app::tests -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `src/app.rs` contains `pub enum DetailPaneFocus`.
    - `src/app.rs` contains `pub enum DetailContentMode`.
    - `src/app.rs` contains `pub fn toggle_detail_focus(&mut self)`.
    - `src/app.rs` contains `pub fn select_next_detail_content(&mut self)`.
    - `src/app.rs` contains `pub fn scroll_detail_down(&mut self, max_lines: usize)`.
    - `src/app.rs` contains `detail_content_mode: DetailContentMode::Logs` in the reset/default path.
  </acceptance_criteria>
  <done>`App` exposes the full state machine needed for focus switching, mode switching, and bottom-pane scrolling, with default Logs mode encoded in code rather than implied by caller behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add unit tests that lock the detail navigation contract</name>
  <files>src/app.rs</files>
  <read_first>src/app.rs, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</read_first>
  <behavior>
    - Test 1: `enter_detail()` makes `detail_content_mode == DetailContentMode::Logs` and `detail_focus == DetailPaneFocus::Top`.
    - Test 2: two calls to `toggle_detail_focus()` return focus to the original pane.
    - Test 3: content-mode helpers flip between `Logs` and `ServiceFile`.
    - Test 4: scroll helpers clamp at zero and at the provided max line index.
  </behavior>
  <action>Add a `#[cfg(test)] mod tests` block in `src/app.rs` with focused unit tests named `detail_view_defaults_to_logs_mode`, `detail_focus_toggles_between_panes`, `detail_content_mode_switches_between_logs_and_service_file`, and `detail_scroll_helpers_clamp_to_valid_bounds`. Instantiate `App::new()` directly in each test, call the new helpers, and assert exact enum values and numeric scroll results with `assert_eq!` so `cargo test app::tests` provides fast coverage for the keyboard contract.</action>
  <verify>
    <automated>cargo test app::tests -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `src/app.rs` contains `mod tests`.
    - `src/app.rs` contains `detail_view_defaults_to_logs_mode`.
    - `src/app.rs` contains `detail_focus_toggles_between_panes`.
    - `src/app.rs` contains `detail_content_mode_switches_between_logs_and_service_file`.
    - `src/app.rs` contains `detail_scroll_helpers_clamp_to_valid_bounds`.
  </acceptance_criteria>
  <done>The app-level navigation rules for DTL-02, NAV-01, NAV-02, and NAV-03 are executable tests that will fail if later plans wire the wrong behavior.</done>
</task>

</tasks>

<verification>
- [ ] `cargo test app::tests -- --nocapture`
- [ ] `cargo build`
- [ ] `rg -n "DetailPaneFocus|DetailContentMode|toggle_detail_focus|detail_view_defaults_to_logs_mode" src/app.rs`
</verification>

<success_criteria>
- `App` owns detail focus and detail content mode as explicit state.
- Entering and exiting detail view reset that state to a known default.
- Unit tests prove the default Logs mode and pane-specific navigation semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/01-detail-pane-mode-switching/01-01-SUMMARY.md`
</output>
